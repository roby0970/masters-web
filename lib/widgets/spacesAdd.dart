import 'package:flutter/material.dart';

class SpacesAdd extends StatelessWidget {
  const SpacesAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: ListView());
  }
}
